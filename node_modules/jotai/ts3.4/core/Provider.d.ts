import React from 'react';
import { Atom, Scope } from './atom';
import { State } from './vanilla';
export declare const Provider: React.FC<{
    initialValues?: Iterable<readonly [
        Atom<unknown>,
        unknown
    ]>;
    scope?: Scope;
}>;
export declare const getDebugStateAndAtoms: ({ atoms, state, }: {
    atoms: Atom<unknown>[];
    state: State;
}) => (State | Atom<unknown>[])[];
export declare const subscribeDebugStore: ({ listeners }: {
    listeners: Set<() => void>;
}, callback: () => void) => () => boolean;
